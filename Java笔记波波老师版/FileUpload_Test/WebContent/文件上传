1.传统的文件上传
表单里，含普通的文本框和文件域
提交表单时，要区别判断，如果是文件域，需要写入文件到磁盘，
然后再返回，写入后的文件名 。


2. 目前常用ajax实现文件上传。
实际是在表单选择文件时候，已经把文件上传到服务器了。
表单提交时候，实际提交的上传后的文件名字。


3.使用KindEditor插件，完成文件上传 。
当上传文本框有值的时候，文件上传已经完成
返回的是文件上传后，保存到服务器的图片的路径。

如果有表单提交，则和以前的表单提交处理方式一样。



、



