1.方法，就是函数 

    //无参，无返回值的方法
     void  f(){
        
        
     }
     //有参，无返回值
     void  f1(int x,double y){
        
        
     }
     
     //无参，有返回值的方法，返回值类型为int 
     int f(){
      
        return 1;
     }
     
     //有参，有返回值的方法
     int sum(int x ,int y){
        return  x+y;
     }
     
     调用无返回值的方法:  f();  f1(1,2.5) ;
     调用有返回值的方法:   x=sum(1,2);   
     
     
     
     